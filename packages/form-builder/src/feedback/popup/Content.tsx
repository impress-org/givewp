const Content = ({children}) => {
    return (
        <div style={{
            fontSize: '14px',
            fontWeight: 400,
            lineHeight: '19.6px',
        }}>
            {children}
        </div>
    )
}

export default Content;
