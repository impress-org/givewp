/**
 * @unreleased
 */
export type FormSettings = {
    formTitle: string;
    enableDonationGoal: boolean;
    enableAutoClose: boolean;
    registration: string;
    goalType: string;
    designId: string;
    primaryColor: string;
    secondaryColor: string;
};
