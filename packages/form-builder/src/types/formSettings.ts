/**
 * @unreleased
 */
export type FormSettings = {
    formTitle: string;
    enableDonationGoal: boolean;
    enableAutoClose: boolean;
    registration: string;
    goalFormat: string;
    designId: string;
    primaryColor: string;
    secondaryColor: string;
};
