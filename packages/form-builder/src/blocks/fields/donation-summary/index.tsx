import settings from './settings';
import {FieldBlock} from '@givewp/form-builder/types';

const donationSummary: FieldBlock = {
    name: 'givewp/donation-summary',
    settings,
};

export default donationSummary;
