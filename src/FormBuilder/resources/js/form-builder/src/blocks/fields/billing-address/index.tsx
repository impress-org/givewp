import settings from './settings';
import {FieldBlock} from '@givewp/form-builder/types';

const billingAddress: FieldBlock = {
    name: 'givewp/billing-address',
    settings,
};

export default billingAddress;
