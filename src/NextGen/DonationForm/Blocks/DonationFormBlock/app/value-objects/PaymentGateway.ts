enum PaymentGateway {
    TEST_GATEWAY = 'test-gateway',
    TEST_GATEWAY_NEXT_GEN = 'test-gateway-next-gen',
    STRIPE_CHECKOUT = 'stripe_checkout',
    STRIPE_CREDIT_CARD = 'stripe',
}

export default PaymentGateway;
