/**
 * @unreleased
 */
const HeaderText = ({children}) => {
    return (
        <div style={{
            color: '#060c1a',
            fontSize: '16px',
            fontWeight: 600,
            lineHeight: '24px',
        }}>
            {children}
        </div>
    )
}

export default HeaderText;
