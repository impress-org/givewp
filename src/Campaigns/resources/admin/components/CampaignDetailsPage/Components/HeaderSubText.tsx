const HeaderSubText = ({
                           children
                       }) => {
    return (
        <div style={{
            fontSize: '14px',
            fontWeight: 400,
            lineHeight: '20px',
            color: '#4B5563',
        }}>
            {children}
        </div>
    )
}

export default HeaderSubText;
